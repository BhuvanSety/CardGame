//This is for the card class
//Currently has one attribute
public class Card {
    private int indexValue;

    public Card(int indexValue){
        this.indexValue = indexValue;

    }

    public int getIndexValue(){
        return indexValue;
    }
}
